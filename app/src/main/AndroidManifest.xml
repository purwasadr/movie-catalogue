<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alurwa.moviecatalogue">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MovieCatalogueApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Moviecatalogue">
        <activity android:name=".boxlist.BoxListActivity"
            android:hardwareAccelerated="true"></activity>
        <activity
            android:name=".tvdetail.TvDetailActivity"
            android:theme="@style/Theme.Moviecatalogue.NoActionBar" />
        <activity
            android:name=".detail.FilmDetailActivity"
            android:theme="@style/Theme.Moviecatalogue.NoActionBar.Detail" />
        <activity
            android:name=".search.SearchActivity"
            android:theme="@style/Theme.Moviecatalogue.NoActionBar.Search" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>